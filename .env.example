# Optimized Versions Server Configuration

# Required - Jellyfin server URL
JELLYFIN_URL=http://your-jellyfin-url

# Optional - Performance settings
# MAX_CONCURRENT_JOBS=1

# Optional - New Cache System (v2)
# CACHE_RETENTION_HOURS=48              # How long to keep cached items (hours)
# CLEANUP_INTERVAL_MINUTES=60           # How often to run cleanup (minutes)

# Optional - Legacy Settings (v1 compatibility)
# REMOVE_FILE_AFTER_RIGHT_DOWNLOAD=true # Legacy: remove files immediately after download
# TIME_TO_KEEP_FILES=8                  # Legacy: hours to keep files
# MAX_CACHED_PER_USER=10                # Legacy: max cached items per user