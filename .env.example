# Optimized Versions Server Configuration

# Required - Jellyfin server URL
JELLYFIN_URL=http://your-jellyfin-url

# Optional - Performance settings
# MAX_CONCURRENT_JOBS=1

# Optional - Cache Management
# CACHE_RETENTION_HOURS=48              # How long to keep cached items (default: 48 hours)
# CLEANUP_INTERVAL_MINUTES=60           # How often to run cleanup (default: 60 minutes)

# Optional - Startup Behavior
# CANCEL_INTERRUPTED_JOBS=false         # Auto-resume interrupted jobs (false) or mark as failed (true) (default: false)